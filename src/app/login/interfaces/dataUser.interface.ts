export interface DataUser {
  name: string;
  lastNameP: string;
  lastNameM: string;
  email: string;
  pregunta: string;
  respuesta: string;
  role: string; 
  url:string // Asegúrate de que la propiedad 'role' esté incluida
}
